using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class MapLibrary
{
    static Color wall = Color.white, 
        blueSpawn = Color.blue, 
        redSpawn = Color.red, 
        pit = Color.magenta, 
        door = Color.clear, 
        boulder = new Color(1, 1, 0, 1);
}
